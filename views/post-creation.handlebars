<header>
    <h1>
        <a href="/post-creation">Just Tech Blog</a>
    </h1>
</header>
<article class="post">
    <div class="title">
        <a href="{{post.post_url}}" target="_blank">{{post.title}}</a>
        <span>({{post.post_url}}</span>
    </div>
    <div class="meta">
        by {{post.user.username}} with description: {{post.text}} on
        {{post.created_at}}

        <a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>
    </div>
</article>